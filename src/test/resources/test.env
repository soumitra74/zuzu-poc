# AWS (LocalStack)
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-1

# S3 bucket for tests
AWS_S3_BUCKET=hotel-reviews

# LocalStack endpoint
AWS_S3_ENDPOINT=http://localhost:4566

# Database (H2 or test DB)
DB_USERNAME=test
DB_PASSWORD=test
DB_URL=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
DB_DRIVER=org.h2.Driver
DB_PLATFORM=org.hibernate.dialect.H2Dialect